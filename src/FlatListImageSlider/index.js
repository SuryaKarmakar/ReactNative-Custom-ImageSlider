import React from 'react';
import {View} from 'react-native';

import Styles from './styles';

const FlatListImageSlider = props => {
  const {data} = props;

  return <View></View>;
};
export default FlatListImageSlider;
